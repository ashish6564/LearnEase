

import 'constants.dart';

class AppDimensions {
  AppDimensions._();

  // static double cpDialogHeight = Constants.ipadBool ? 400 : 350;
  // static double cpDialogHeight = 350;
  static double progressIndicatorSize = Constants.ipadBool ? 40 : 25;
  static double progressIndicatorStrokeWidth = Constants.ipadBool ? 4 : 3;
  static double fontSizePleaseWait = Constants.ipadBool ? 16 : 15;

}
